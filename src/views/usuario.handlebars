<div>
  <h1>Hola {{usuario.name}}!!tu edad es {{usuario.edad}}</h1>
  {{#if usuario.isAdmin}}
    <h2>Si eres admin</h2>
    <h3>listado de usuarios:</h3>
    {{#each usuarios}}
    <div>
      <p>nombre {{this.name}} edad {{this.edad}}</p>
    </div>
    {{/each}}
  {{else}} 
    no eres admin, no se muestra la lista
  {{/if}}
</div>